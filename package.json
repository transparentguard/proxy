{
  "name": "transparentguard",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "build": "pnpm --filter @transparentguard/runtime run build && pnpm --filter @transparentguard/proxy run build",
    "typecheck": "pnpm -r run typecheck"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["protobufjs"]
  }
}
